{
  "name": "detama-storefront",
  "version": "1.0.0",
  "private": true,
  "description": "DeTama Landing Page & Headless Checkout System — Monorepo",
  "workspaces": [
    "frontend",
    "api-worker",
    "shared"
  ],
  "scripts": {
    "dev:frontend": "npm run dev --workspace=frontend",
    "dev:worker": "npm run dev --workspace=api-worker",
    "build:frontend": "npm run build --workspace=frontend",
    "build:worker": "npm run build --workspace=api-worker",
    "build": "npm run build:frontend && npm run build:worker",
    "typecheck": "npm run typecheck --workspace=frontend && npm run typecheck --workspace=api-worker",
    "test": "npm run test --workspace=api-worker"
  },
  "engines": {
    "node": ">=20.0.0"
  }
}
